jwt:
  secret: "secret"

server:
  error:
    include-message: always
    include-binding-errors: always
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todoList
    username: MihkelVaino
    password: Mysql123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        show_sql: false
    open-in-view: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: "mihkeldevmail@gmail.com"
    password:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true